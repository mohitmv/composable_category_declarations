#include "perfetto_sdk/tracing.h"
// #include "liba.h"

#include <iostream>

// void TopLevelFunc() {
//   LibA_Func1();
//   LibA_Func2();
//   LibA_Func3();
// }

// template<size_t id>
// struct S {
//   static void Print() { std::cout << "id = " << id << std::endl; }
// };

int main() {
  // // struct t1;
  // // std::cout << perfetto::internal::GetCategoryId<t1>("libA_Cat3") << std::endl;
  // perfetto::TrackEvent::Register();
  // auto session = perfetto::StartSession();
  // TopLevelFunc();
  // auto trace = session.StopAndReturnTrace();
  // std::cout << "Trace = \n";
  // perfetto::Print(trace);
  // auto trace2 = perfetto::TraceProcess(trace);
  // std::cout << "---------\nProcessed Trace = \n";
  // perfetto::Print(trace2);
  // std::cout << "---------\n";
}
